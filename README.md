# GX1 BRD Platform

Pixel-close Next.js recreation of the GX1 BRD Intake & Registration screen.

Run `npm install` then `npm run dev` and open `http://localhost:3000`.

The form includes required-field validation, counters, tag management, reviewer removal, file upload/drop validation, refresh feedback, responsive navigation, and submission feedback.
